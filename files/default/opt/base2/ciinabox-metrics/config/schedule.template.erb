<%  frequencies.each do |frequency| %>

  <%=frequency%> do
    # Install <%= frequency %> cron
    rake "ciinabox:process_metric_cron[\"<%=frequency%>\"]"
  end

<% end %>

every 1.hour do
  # Update rake tasks
  rake "ciinabox:install_metrics"
end